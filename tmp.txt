self.setGeometry(100, 100, 900, 700)

self.setWindowTitle(
    "Analisis Sentimen Komentar Vaksinasi Covid-19 di Instagram Menggunakan Deep Learning XLNet"
    )

self.datasetpath_label = QLabel("Dataset Path")
self.modelpath_label = QLabel("Model Path")

self.epoch_label = QLabel("Epoch")
self.batchsize_label = QLabel("Batch Size")

self.train_button = QPushButton("Fine Tune")
self.test_button = QPushButton("Test")

self.datasetpath_label.setFixedHeight(30)
self.modelpath_label.setFixedHeight(30)

self.epoch_label.setFixedHeight(30)
self.batchsize_label.setFixedHeight(30)

self.train_button.setFixedWidth(100)
self.test_button.setFixedWidth(100)

layout = QVBoxLayout()

top_layout = QHBoxLayout()
mid_layout = QHBoxLayout()
btm_layout = QHBoxLayout()

top_widget = QWidget()
mid_widget = QWidget()
btm_widget = QWidget()

top_widget.setFixedHeight(140)
mid_widget.setFixedHeight(210)
btm_widget.setFixedHeight(350)

top_rows = QVBoxLayout()
mid_rows = QVBoxLayout()

# top_rows_widget = QWidget()
# mid_rows_widget = QWidget()

fst_top_row = QHBoxLayout()
sec_top_row = QHBoxLayout()

fst_mid_row = QHBoxLayout()
sec_mid_row = QHBoxLayout()
trd_mid_row = QHBoxLayout()

container = QWidget()

fst_top_row.addWidget(self.datasetpath_label)
sec_top_row.addWidget(self.modelpath_label)

fst_mid_row.addWidget(self.epoch_label)
sec_mid_row.addWidget(self.batchsize_label)        
trd_mid_row.addWidget(self.train_button)
trd_mid_row.addWidget(self.test_button)

top_rows.addLayout(fst_top_row)
top_rows.addLayout(sec_top_row)

mid_rows.addLayout(fst_mid_row)
mid_rows.addLayout(sec_mid_row)
mid_rows.addLayout(trd_mid_row)

top_layout.addLayout(top_rows)
mid_layout.addLayout(mid_rows)

layout.addLayout(top_layout)
layout.addLayout(mid_layout)
layout.addLayout(btm_layout)

top_widget.setLayout(top_layout)
mid_widget.setLayout(mid_layout)
btm_widget.setLayout(btm_layout)

container.setLayout(layout)

# self.setFixedSize(QSize(400, 300))

# Set the central widget of the Window.
self.setCentralWidget(container)